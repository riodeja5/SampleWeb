@model SumidaWeb.Models.User

@{
    ViewBag.Title = "ライセンス発行";
}

<h2>ライセンス発行</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        <h4>User</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        <div class="form-group">
            @Html.LabelFor(model => model.Id, "ユーザー名", htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
            @Html.DropDownList("Id", null, htmlAttributes: new { @class = "form-control", id = "StateSelectList"})
@*
    @Html.DropDownListFor(model => model.Id, null, htmlAttributes: new { id = "StateSelectList" })
*@
                @Html.ValidationMessageFor(model => model.Id, "", new { @class = "text-danger" })
            </div>
        </div>
        <div id="result"></div>
    </div>
}

<div>
    @Html.ActionLink("Back to License", "../License")
</div>

@section scripts {
    <script>
    $(function () {
        $("#StateSelectList").on('change', function (e) {
            @*
                $('#result').load('/Issue/FabSearch/' + $(this).data('category'))
            *@
            $('#result').load('/Issue/FabSearch/' + $(this).val())
        });
    });
    </script>
}